# Build artefacts
build/
bin/
*.o
*.d
*.a
*.dSYM/

# Compiled Solum bytecode
*.sob

# What `make dist` writes
dist/
*.tar.gz

# Editor / OS
.DS_Store
*.swp
compile_commands.json

# scratch/ is Hans's own: notes and the daily standup, not part of the
# repository. Nothing here reads it.
scratch/
