; conformance: a file that defines a global and prints nothing is a program like any other
; varies: front
;
; It is here to be included by the two legal neighbours in this directory, and it
; is a case in its own right so that the tree stays readable one way: every .sol
; under conformance/ is a case, and nothing is skipped by a rule the harness has
; to be told.

partValue := #7.
